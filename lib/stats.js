let itemCounts = require('./itemCounts')
let bookLength = require('./length')

let b = textLength

let bands = ['kayden', 'loote', 'gryffin', 'kayden', 'slander', 'loote', 'nghtmare', 'kayden'];

function statsOfArray {
    let stats = arrayStats(bands)
    return stats
}

module.exports = statsOfArray;
